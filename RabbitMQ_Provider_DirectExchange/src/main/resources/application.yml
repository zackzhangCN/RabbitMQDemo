server:
  port: 8081
spring:
  # 应用名
  application:
    name: RabbitMQ_Provider_DirectExchange
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest
    # 开启消息确认(消息发送到交换器的确认)
    publisher-confirms: true
    # 开启消息确认(消息从交换器路由到队列的确认)
    publisher-returns: true